#env
env=${env}

# MySQL
mysql.host=${mysql.host}
mysql.port=${mysql.port}
mysql.url=${mysql.url}
mysql.timezone=${mysql.timezone}
mysql.database=${mysql.database}
mysql.databases.conf=${mysql.databases.conf}
mysql.user=${mysql.user}
mysql.pwd=${mysql.pwd}
mysql.spider.db=${mysql.spider.db}

# Redis
redis.host=${redis.host}
redis.port=${redis.port}
redis.user=${redis.user}
redis.pwd=${redis.pwd}
redis.blacklist.db=${redis.blacklist.db}

# Hologres
holo.endpoint=${holo.endpoint}
holo.database=${holo.database}
ali.key=${ali.key}
ali.pwd=${ali.pwd}

# kafka cdh
kafka.bootstrap.servers=cdh01:9092,cdh02:9092,cdh03:9092
REALTIME.KAFKA.LOG.TOPIC=realtime_log
kafka.topic.db=${kafka.topic.db}
kafka.dirty.topic=dirty_log
kafka.err.log=err_log
kafka.start.log=start_log
kafka.display.log=display_log
kafka.action.log=action_log
kafka.page.topic=ods_page_log
kafka.cdc.db.topic=ods_ecommerce_order
kafka.db.fact.comment.topic=${kafka.db.fact.comment.topic}
kafka.result.sensitive.words.topic=${kafka.result.sensitive.words.topic}
kafka.result.label.base6.topic=${kafka.result.label.base6.topic}
kafka.result.label.base4.topic=${kafka.result.label.base4.topic}
kafka.result.label.base2.topic=${kafka.result.label.base2.topic}
kafka.result.label.baseline.topic=${kafka.result.label.baseline.topic}

# Push Message Feishu
push.feishu.url=${push.feishu.url}

# file log
REALTIME.LOG.FILE.PATH=${REALTIME.LOG.FILE.PATH}
REALTIME.MSG.POSITION.FILE.PATH=${REALTIME.MSG.POSITION.FILE.PATH}

# Zookeeper Server
zookeeper.server.host.list=${zookeeper.server.host.list}

# Hbase
hbase.namespace=${hbase.namespace}

# FLINK
flink.submit.user=${flink.submit.user}
flink.conf.configurationDirectory=${flink.conf.configurationDirectory}
flink.cluster.libs=${flink.cluster.libs}
flink.checkpoint.hdfs.dir=${flink.checkpoint.dir}
flink.checkpoint.minio.dir=${flink.checkpoint.minio.dir}

# silicon_api_token
silicon.api.token=${silicon.api.token}


# Hive
hive.conf.dir=${hive.conf.dir}